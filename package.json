{
  "name": "clowk-terminal",
  "version": "0.0.1",
  "description": "Clowk color schemes for terminals — one palette, many formats (Ghostty, cmux, iTerm2, Terminal.app, Alacritty, kitty, WezTerm, Windows Terminal).",
  "type": "module",
  "license": "MIT",
  "author": "clowk",
  "homepage": "https://github.com/thadeu/clowk-terminal",
  "repository": {
    "type": "git",
    "url": "https://github.com/thadeu/clowk-terminal.git"
  },
  "scripts": {
    "import": "bun run scripts/import-from-vscode.ts",
    "build": "bun run src/build.ts",
    "verify": "bun run scripts/verify.ts",
    "all": "bun run import && bun run build && bun run verify",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.18.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.20.0"
  }
}
